/**
 * An interface that describes option objects. Options are designed as generic objects that communicate preferences from a client
 * to a service for processing an OntoUML project.
 */
export interface ServiceOptions {
  [key: string]: any;
}
