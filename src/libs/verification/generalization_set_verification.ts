import { GeneralizationSet } from '@libs/ontouml';
import { VerificationIssue } from './';

export class GeneralizationSetVerification {
  static verifyGeneralizationSet(_generalizationSet: GeneralizationSet): VerificationIssue[] {
    const foundIssues: VerificationIssue[] = [];
    return foundIssues;
  }
}
