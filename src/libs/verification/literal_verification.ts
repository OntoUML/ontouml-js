import { Literal } from '@libs/ontouml';
import { VerificationIssue } from './';

export class LiteralVerification {
  static verifyLiteral(_literal: Literal): VerificationIssue[] {
    const foundIssues: VerificationIssue[] = [];
    return foundIssues;
  }
}
